description: Contact successfully deleted
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 204
        message:
          type: string
          example: 'Contact deleted successfully'
